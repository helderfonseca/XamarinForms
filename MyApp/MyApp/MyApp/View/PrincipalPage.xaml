<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyApp.View.PrincipalPage">


    <ContentPage.Content padding="10">
        <StackLayout>
            <Label Text="{Binding Nome}" TextColor="Black" FontSize="20" />
            <Label Text="{Binding Email}"  TextColor="Black" FontSize="20" />
            <Label Text="{Binding Dt_nasc}" TextColor="Black" FontSize="20" />
            <!--<ListView ItemsSource="{Binding Pessoa}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Nome}" TextColor="#C032"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>-->
        </StackLayout>
    </ContentPage.Content>
</ContentPage>